[package]
name = "zed_auto_commit"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[lib]
crate-type = ["cdylib"] # Required for Zed extensions.

[dependencies]
schemars = "1.2.2"
serde = { version = "1", features = ["derive"] }
zed_extension_api = "0.7.0"

[profile.dev]
incremental = false
